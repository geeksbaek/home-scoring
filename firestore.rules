rules_version = '2';

// 즐겨찾기/설정 동기화 — 사용자는 자기 문서(users/{uid})만 읽고 쓸 수 있다.
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{uid} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
