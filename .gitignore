node_modules
dist
bun.lock
*.local
.DS_Store
data/molit_press_attachments/**/_tmp_*

# 민감정보
.env

public/data*.json

# 파이프라인 생성 데이터 (대용량 — gh-pages로만 배포, main은 가볍게)
/data/*.json
!/data/molit_press_releases.json
# 임시/디버그 + 비밀(프록시 토큰 등)
/data/_*
